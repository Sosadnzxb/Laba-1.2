#include <stdio.h>

int main()
{
	int a, b;
	printf("Input number for a,b\n");
	printf("a=");
	scanf_s("%d", &a);
	printf("b=");
	scanf_s("%d", &b);
	if (a == -b)
		printf("Opposite numbers");
	else
		printf("Not opposite numbers");
	return 0;
}
